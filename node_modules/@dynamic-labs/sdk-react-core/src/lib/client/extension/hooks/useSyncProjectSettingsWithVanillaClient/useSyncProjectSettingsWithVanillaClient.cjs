'use client'
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var core = require('@dynamic-labs-sdk/client/core');
var projectSettings = require('../../../../store/state/projectSettings/projectSettings.cjs');
var client = require('../../../client.cjs');

const useSyncProjectSettingsWithVanillaClient = () => {
    const projectSettings$1 = projectSettings.useProjectSettings();
    /**
     * This useEffect is used to sync the project settings to vanilla SDK.
     */
    React.useEffect(() => {
        core.getCore(client.getDynamicClient()).state.set({
            projectSettings: projectSettings$1 !== null && projectSettings$1 !== void 0 ? projectSettings$1 : null,
        });
    }, [projectSettings$1]);
};

exports.useSyncProjectSettingsWithVanillaClient = useSyncProjectSettingsWithVanillaClient;
